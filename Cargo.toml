[package]
name = "tango-question"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.5"}
csv = {version = "1.3"}
serde = {version = "1", features = ["derive"]}
rayon = {version = "1.10"}

